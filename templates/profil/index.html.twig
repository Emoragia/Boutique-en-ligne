{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <div class="container mx-auto p-5" style="width: 50%">
        <div class="card bg-light bg-gradient text-center shadow">
            <div class="row">
{#                <div class="col-md-4 align-self-center">#}
{#                    <img src="{{ asset('images/user.png') }}" class="rounded-start" alt="logo_user " style="width: 210px">#}
{#                </div>#}
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">{{ profilConsulte.username }}</h5>
                        <p> <strong>Prénom : </strong>{{ profilConsulte.firstname }}</p>
                        <p><strong>Nom : </strong>{{ profilConsulte.lastname }}</p>
                        <p><strong>Email : </strong>{{ profilConsulte.email }}</p>
                        <a href="{{ path('app_main') }}"><button type="button" class="btn btn-primary" style="width: 30%;">Retour</button></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
